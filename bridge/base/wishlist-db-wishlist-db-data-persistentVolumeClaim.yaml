#! wishlist-db-wishlist-db-data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: wishlist-db-wishlist-db-data
    namespace: ecommerce-microservices
    labels:
        com.docker.compose.service: wishlist-db
        com.docker.compose.volume: wishlist-db-data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
